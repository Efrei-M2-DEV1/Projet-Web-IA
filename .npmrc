# ===========================
# 📦 Configuration npm du projet
# ===========================

# 🔒 Sécurité : Utiliser uniquement le registre npm officiel
registry=https://registry.npmjs.org/

# 🚫 Bloquer l'installation si le lockfile est désynchronisé
package-lock=true

# ✅ Utiliser les versions exactes (pas de ^ ou ~)
save-exact=true

# 🛡️ Désactiver les scripts post-install non sûrs
ignore-scripts=false

# 📊 Afficher les logs de progression
loglevel=info

# 🔧 Désactiver les audits automatiques (optionnel, gagne du temps)
audit=false

# 💾 Cache npm optimisé (réduire les téléchargements)
cache=.npm-cache

# 🌳 Hoisting des dépendances (optimise node_modules)
hoist=true

# 🚀 Utiliser npm workspaces pour monorepo
workspaces=true